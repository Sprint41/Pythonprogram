#Get a numeric string from the user and convert it into an integer. Print the integer value.
string = str(10)
print('type before conversion is:',type(string))
converted_string= int(string)
print('type after conversion is:',type(converted_string))
