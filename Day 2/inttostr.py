#Take an integer input from the user and convert it into a string. Then, print the string.
num = int(50)
print('type before conversion is:', type(num))
converted_num= str(num)
print('type after conversion is:',type(converted_num))
